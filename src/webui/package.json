{
  "name": "netproxy-terminal",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check": "tsc -b && node --test tests/*.test.mjs",
    "build": "npm run check && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "kernelsu": "^3.0.2"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
